import {Parent} from "./parent";
import {Address} from "./address";
import {Child} from "./child";

export interface Info {
    parent1: Parent;
    parent2: Parent;
    address: Address;
    child: Child;
}